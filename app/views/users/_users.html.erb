<% if users.any? %>
    <ul class="list-unstyled">
        <% users.each do |user| %>
            <li class="mb-3 d-flex">
                <div>
                    <div class="rounded me-2" alt="プロフィール画像">
                        <%= image_tag users.image.to_s %></td>
                    </div>
                    <div>
                        <p><%= link_to "#{user.name}", user_path(user), class: "text-decoration-none" %></p>
                    </div>
                </div>
            </li>
        <% end %>
    </ul>
    <%== pagy_bootstrap_nav(@pagy) %>
<% end %>