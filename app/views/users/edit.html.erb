<div class="text-center">
    <h1>プロフィール設定</h1>
</div>

<div class="row user_edit">
    <div class="col-sm-6 offset-sm-3">
        
        <%= form_with(model: @user) do |f| %>
            <%= render 'layouts/error_messages', model: f.object %>
        
            <div class="mb-3">
                <h5><%= f.label :name, 'アカウント名' %></h4>
                <%= f.text_field :name, class: 'form-control' %>
            </div>
            
            <div class="mb-3">
                <h5><%= f.label :image, 'プロフィール画像' %></h4>
                <%= f.file_field :image %> 
            </div>
            
            <div class="mb-3">
                <h5><%= f.label :self_introduction, '自己紹介' %></h4>
                <%= f.text_field :self_introduction, class: 'form-control' %>
            </div>
            
            <div class="mb-3">
                <h5><%= f.label :email, 'メールアドレス' %></h4>
                <%= f.email_field :email, class: 'form-control' %>
            </div>
            
            <div>
                <%= f.submit '変更', class: 'btn btn-primary w-50 d-block mx-auto' %>
            </div>
        <% end %>
    </div>
</div>